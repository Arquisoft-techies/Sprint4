package com.example.request_manager.controller;

// import com.example.request_manager.service.RequestLogicService;
// import org.springframework.beans.factory.annotation.Autowired;
// import org.springframework.http.HttpStatus;
// import org.springframework.http.ResponseEntity;
// import org.springframework.web.bind.annotation.*;

// @RestController
// @RequestMapping("/api/solicitudes")

// public class SolicitudController {
// @Autowired
// private RequestLogicService requestLogicService;

// @PostMapping("/crear")
// public ResponseEntity<?> crearSolicitud(@RequestParam Long id, @RequestParam
// String tipo, @RequestParam boolean estado) {
// String respuesta = requestLogicService.crearSolicitud(id, tipo, estado);
// return ResponseEntity.status(HttpStatus.CREATED).body(Map.of("mensaje",
// respuesta));
// }
// }
